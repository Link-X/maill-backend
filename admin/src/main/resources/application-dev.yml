server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticket_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

snowflake:
  worker-id: 1
  data-center-id: 2

admin:
  # 管理后台 API Key,dev 环境默认值仅供本地调试;生产必须通过 ADMIN_API_KEY 环境变量覆盖
  api-key: ${ADMIN_API_KEY:dev-admin-token-please-change-in-prod}

logging:
  level:
    com.ticket: DEBUG
