<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ticket.core.mapper.ShowReviewLikeMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO show_review_like (review_id, user_id, create_time, update_time)
        VALUES (#{reviewId}, #{userId}, NOW(), NOW())
    </insert>

    <delete id="delete">
        DELETE FROM show_review_like WHERE review_id = #{reviewId} AND user_id = #{userId}
    </delete>

    <select id="countLike" resultType="int">
        SELECT COUNT(*) FROM show_review_like WHERE review_id = #{reviewId} AND user_id = #{userId}
    </select>

    <select id="selectLikedReviewIds" resultType="long">
        SELECT review_id FROM show_review_like
        WHERE user_id = #{userId} AND review_id IN
        <foreach collection="reviewIds" item="rid" open="(" close=")" separator=",">#{rid}</foreach>
    </select>

</mapper>
