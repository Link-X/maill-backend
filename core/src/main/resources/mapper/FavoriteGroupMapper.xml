<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ticket.core.mapper.FavoriteGroupMapper">

    <resultMap id="GroupMap" type="com.ticket.core.domain.entity.FavoriteGroup">
        <id     column="id"          property="id"/>
        <result column="user_id"     property="userId"/>
        <result column="name"        property="name"/>
        <result column="sort"        property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.ticket.core.domain.entity.FavoriteGroup"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite_group (user_id, name, sort, create_time, update_time)
        VALUES (#{userId}, #{name}, #{sort}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.ticket.core.domain.entity.FavoriteGroup">
        UPDATE favorite_group
        SET name = #{name}, sort = #{sort}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM favorite_group WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="GroupMap">
        SELECT id, user_id, name, sort, create_time, update_time
        FROM favorite_group WHERE id = #{id}
    </select>

    <select id="selectByUserName" resultMap="GroupMap">
        SELECT id, user_id, name, sort, create_time, update_time
        FROM favorite_group WHERE user_id = #{userId} AND name = #{name}
    </select>

    <select id="selectByUser" resultMap="GroupMap">
        SELECT id, user_id, name, sort, create_time, update_time
        FROM favorite_group WHERE user_id = #{userId}
        ORDER BY sort ASC, id ASC
    </select>

    <update id="unsetGroupForFavorites">
        UPDATE user_favorite
        SET group_id = NULL, update_time = NOW()
        WHERE user_id = #{userId} AND group_id = #{groupId}
    </update>

</mapper>
